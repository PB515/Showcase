Drop the Veo climax clip here:
  climax.mp4         (required — H.264 MP4, 5-9s, static camera, 1080p+)
  climax.webm        (optional — smaller, nice-to-have)
  climax-poster.jpg  (the first frame, for reduced-motion / loading fallback)
Use these EXACT filenames so the code picks them up with no rewiring.
